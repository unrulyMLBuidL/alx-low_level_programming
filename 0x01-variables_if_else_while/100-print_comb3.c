#include <stdio.h>

/**
 * main - entry point
 *
 * DES: print all possible combinations of numbers with ','.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a, b;

	for (a = 0; a < 10; a++)
		for (b = 0; b < 10; b++)
		{
			if (a,b != )
			putchar(a,b + '0','0');
		}
	return (0);
}
