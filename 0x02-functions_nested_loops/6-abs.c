#include "main.h"

/**
 * _abs: Entry point.
 * @i: variable
 * _abs function - computes the absolute value of int.
 * return: int value
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	
	else if (i < 0)
	{
		return (-i);
	}
	
	else
	return (i);
}
