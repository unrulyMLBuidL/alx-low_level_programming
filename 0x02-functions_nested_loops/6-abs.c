#include "main.h"

/**
 * _abs - Entry point.
 * @i: n is an integer
 * _abs function - computes the absolute value of int.
 * Return: int
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}

	else if (i < 0)
	{
		return (-i);
	}

	else
	return (i);
}
